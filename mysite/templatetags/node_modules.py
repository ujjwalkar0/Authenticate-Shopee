from django import template
from django.conf import settings

register = template.Library()

@register.simple_tag
def node_static(path):
    """
    Returns the URL for a file in node_modules.
    Usage: {% node_static 'bootstrap/dist/css/bootstrap.min.css' %}
    """
    return f'{settings.NODE_MODULES_URL}{path}'
